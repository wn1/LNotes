package ru.qdev.lnotes.core

object QDVAppConst {
    const val NoteAddingId = -1L

    const val DayInMs = 1000L * 60 * 60 * 24

    const val NextShowTimeLaterDelay = DayInMs * 6
    const val NextShowTimeIHelpedDelay = DayInMs * 30 * 6
    const val NextShowTimeNotWantDelay = DayInMs * 30 * 6
}